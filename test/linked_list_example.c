#include "../headers/linked_list/linked_list.h"

int main(int argc, char *argv[])
{
  /* Create a List and a list element */
  List list;
  ListElmt *element;

  int *data, i;

  list_init(&list, free);

  return 0;
}
